{
  "name": "planner",
  "version": "0.0.0",
  "description": "Planner UI",
  "main": "index.js",
  "dependencies": {
    "gulp": "^3.8.10"
  },
  "devDependencies": {
    "gulp-sass": "^1.3.2",
    "gulp-webserver": "^0.9.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
